define(function (require) {
    // Load any app-specific modules
    // with a relative require call,
    // like:
    var attributes = require('./attributes');

    // Load library/vendor modules using
    // full IDs, like:
});
stati = JSON.parse(attributes)
state_choose = (stato) => {
  return stati[stato]
}
